// Points at the local wrangler dev server by default. Swap to the deployed
// Worker URL once it's live (becomes an env-driven build step if this ever
// needs more than one target — not needed yet).
const API_BASE_URL = "http://localhost:8787";
